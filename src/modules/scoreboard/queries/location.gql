# GraphQL queries
query getLocation($locationName: String!) {
  Location(name: $locationName) {
    id,
    name,
    subjects(orderBy: sequence_ASC) {
      id,
      name,
      score,
      image,
      slider
    }
  }
}